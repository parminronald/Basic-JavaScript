/* 
  ! Syntax `for` memiliki empat bagian:
    1. Inisialisasi
    2. Kondisional (hasil nilai true / false)
    3. Increment/Decrement
    4. Kode program yang akan dieksekusi 
*/
var input = "Palang Merah Indonesia";
for (var i = 0; i < input.length; i++) {
  console.log(input[i]);
}
